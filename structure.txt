local_game_tracker/
│
├── local_game_tracker.csproj     # Your project file
├── Program.cs                    # Main entry point for configuring and running the web app
├── Startup.cs (optional)         # If using older versions (before .NET 6)
│
├── Controllers/
│   └── HomeController.cs         # Handles web requests (MVC pattern)
│
├── Models/
│   └── Game.cs                   # C# classes representing your data (Game, User, etc.)
│
├── Views/                        # Only if you're using Razor Pages or MVC with Views
│   └── Home/
│       └── Index.cshtml          # HTML templates (Razor)
│
├── wwwroot/                      # Static files (CSS, JS, images)
│   ├── css/
│   ├── js/
│   └── images/
│
├── appsettings.json              # App configuration (database, settings, etc.)
└── Properties/
    └── launchSettings.json       # Debug settings, environment, etc.
